import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  image: {
    borderWidth: 1,
    borderColor: '#F35BAC',
    borderRadius: 50,
    padding: 3,
  },
});

export default style;
